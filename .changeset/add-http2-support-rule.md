---
"@thecodepace/fastify-skills": minor
---

Add HTTP/2 support best-practice rule covering HTTP/2 over TLS with `allowHTTP1` fallback, plain-text HTTP/2 (h2c) for internal services, reusable build-server factory with HTTP/2 configuration, self-signed certificate generation for local development, and known limitations.
