---
"@thecodepace/fastify-skills": minor
---

Add Fastify Deployment best-practice rule covering graceful shutdown with `close-with-grace`, health check and readiness routes, listen on `0.0.0.0`, trust proxy configuration, Docker multi-stage builds, and serverless deployment with `@fastify/aws-lambda`.
