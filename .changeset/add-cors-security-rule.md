---
"@thecodepace/fastify-skills": minor
---

Add CORS & Security Headers best-practice rule covering `@fastify/cors` origin allow-lists, `@fastify/helmet` security headers, `@fastify/rate-limit`, plugin registration order, per-route CORS overrides, and common mistakes like wildcard origin with credentials.
