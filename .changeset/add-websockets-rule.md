---
"@thecodepace/fastify-skills": minor
---

Add WebSockets rule covering `@fastify/websocket` plugin setup, route handlers with `websocket: true`, connection lifecycle and cleanup, broadcasting across clients, JWT authentication via `preValidation`, and `injectWS` testing