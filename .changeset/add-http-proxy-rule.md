---
"@thecodepace/fastify-skills": minor
---

Add HTTP Proxy rule covering `@fastify/http-proxy` for full prefix proxying, `@fastify/reply-from` for `reply.from()` with header rewrites and per-request options, `preHandler` auth with proper short-circuit, multi-upstream API gateway patterns, WebSocket proxying, timeout configuration, and inject() tests